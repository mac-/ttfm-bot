var assert = require('assert'),
	ExtendedBot = require('../lib-test/ExtendedBot.js'),
	//bot = new ExtendedBot('adsf', 'asdf', 'asdf'),
	_ = require('underscore');

describe('ExtendedBot Unit Tests', function() {
	describe('registerAddOn()', function() {
		
		it('should provide a registerAddOn method', function(done) {
			//assert(_.isFunction(bot.registerAddOn), 'The registerAddOn property is a function');
			done();
		});

	});

});